# APACE-Cal v2 方法冻结与确认性实验红线

冻结时间：2026-08-20（UTC）

## 科学问题与候选方法

科学问题保持不变：**跨数据集电池寿命预测中的非对称、协议感知少样本目标域校准**。MATR 盲测否决的是 v1 算法，不是否决或更换该选题。

候选方法仍称 **APACE-Cal**。v2 的核心不是增加一个黑箱模型，而是修复 v1 暴露出的两个可辨识性问题：

1. `K=1` 时，无标签几何中心不保证是寿命代表点，因此一律使用预先匹配的随机支持强基线；
2. 高协议离散度队列中，协议距离与早期退化曲线距离是否一致，决定设施点的标签应进行局部传播还是仅用于稳健位置校准。

## 冻结任务协议

- 全部目标电芯可见前 `H∈{10,20,50}` 周期特征；
- 每个 episode 先按固定种子划分 70% acquisition pool 与共同 test pool；
- 标签预算是整个目标域总计 `K∈{1,3,5,10}`，不是每个查询 K 个标签；
- acquisition、路由和预测器选择均不得读取 test EOL；
- 每个电芯先跨 episode 聚合为一个预测单位，再按域宏平均；禁止 checkpoint 或 episode 伪重复。

## 冻结算法

1. 沿用 v1 的 5 维结构化协议向量、56 维早期曲线签名、稳健尺度、距离定义和确定性 facility-location 获取。
2. 计算目标域协议距离中位数 `D_p` 与协议/曲线距离上三角元素的 Spearman 一致性 `ρ`；两者只依赖无标签前 H 周期数据。
3. 路由固定为：
   - `D_p=0`：回退；
   - `0.30≤D_p<0.60`：回退；
   - `K=1`：回退；
   - `D_p≥0.60 且 K≥5`：回退；
   - `0<D_p<0.30 且 K∈{3,5,10}`：以 `w=2` 设施选择，以固定带宽 `0.5` 做 log-life 核校准；
   - `D_p≥0.60 且 K=3`：以 `w=0.5` 选择三个设施；若 `ρ<0.35`，预测为三个支持寿命的中位数，否则以 `w=0.5`、带宽 `0.5` 做 log-life 核校准。
4. 所有回退分支与外部开发域选出的随机支持强基线共享完全相同的支持身份和预测器，要求逐位相同。
5. 强基线候选包含 v1 的全部核/均值预测器及随机支持中位数，只按外层目标域之外的开发域宏 MAPE、再以宏 MAE 打破平局。
6. 不允许用目标域标签选择路由、阈值、距离权重、带宽、获取方式或预测器。

## 开发证据与失败披露

- v1 在真正未见的 MATR 域发生 H20/K1 `+174.94%`、H20/K3 `+112.36%` 的 MAPE 恶化，已被否决；MATR 从此仅是 v2 的已见失败开发域，绝不再称为确认域。
- v2 在原六域、100 个共同 episode、全部 12 个 H×K 设置中均为 0 个域恶化；K3 宏 MAPE 为：H10 `43.2863→24.2773`，H20 `44.4487→22.4691`，H50 `48.5352→22.9422`。
- 加入已见 MATR 的七域事后审计中，K3 三个 H 设置均为 `4 改善 / 3 持平 / 0 恶化`；宏 MAPE 分别为 `42.812→26.165`、`43.817→24.618`、`47.156→24.958`。
- 高离散度 K3 的 `ρ` 阈值在 `0.30、0.35、0.40` 三点得到完全相同路由和结果，9/9 个域×H 设置改善，最差相对变化为 `-4.71%`；`0.25` 与 `0.45` 会失败，因此冻结中央值 `0.35`。
- 七域 K3 的层级 bootstrap MAPE 差值 95% CI 下界均大于 0，但单侧域级 Wilcoxon `p=0.0625`；受有效域数和三个严格回退域限制，不宣称域级显著性已经得到确认。
- 已否决且不得恢复的分支：高离散度固定中位数、支持集二元 LOO、按支持寿命范围门控、混入随机锚点、固定核—中位数混合，以及 v1 冻结文件列出的全部失败分支。

## 新确认域与一次性红线

确认域预先指定为 **ISU-ILCC**，选择依据仅来自不含寿命值的官方 README：240 个电芯、202 种充放电协议，最有机会检验高离散度主动机制。其官方归档信息预登记为：

- Zenodo v12 文件：`ISU_ILCC.zip`
- 大小：`10,063,843,011` bytes
- MD5：`98c0561ff25eb68538572c54aeb279ea`

执行顺序固定：完整归档校验 → 仅词法读取标签 JSON 顶层键（跳过所有值）→ 生成并哈希预标签 manifest → 仅当 manifest 至少包含一个主动设置时，才允许 evaluator 第一次语义读取寿命值并一次性执行全部 12 个设置。

确认门槛：

1. 主动设置不得出现任一 H 上的目标域 MAPE 恶化；
2. 至少一个主动设置相对强基线改善 ≥10%；
3. 所有回退设置必须与强基线逐位相同；
4. 无论成败均保留全部设置、每电芯误差和 manifest，不删失败 H/K；
5. 揭盲后不得再修改本文件中的 `0.30/0.60/0.35`、K/H、权重、带宽、支持选择或预测规则并仍称 ISU-ILCC 为盲测。

若 ISU-ILCC 在预标签阶段全回退，则它只验证安全性，不打开标签，并改用另一个仍未查看寿命结果、且预标签 manifest 有主动分支的数据域；不得依据寿命值挑域。

## 冻结哈希

- v2 方法脚本：`438a0eeff5091e7bc65c2ed79eafafc9100e007c23800630c36adf90f6f9549b`
- v1 方法依赖：`6a41c9b965559056d74a0c22bb641d52ee1bc5cc9e69da04a48f9308d6a38e1f`
- 六域 100-seed 结果：`101c0d9b3161fb32c3ac80df8efc2a08089caddea82facfaba68bfbe323bbd65`
- 七域审计脚本：`f3a4531c3d92cd3f82af9618a14c68accb8fedc9b2ccaf22651c1bca5e7c2af8`
- 七域审计结果：`5b83cf7bde0c135aff10c544be909e7a30babf275eca2548440696acfe93daaf`
- v2 预标签脚本：`d8e1ea7f934979ac148170c130dffa018fcc5ee8e04232c158e6e52bdcb44ce9`
- v2 一次性 evaluator：`27369dda839c6a3737c15b8bbac96a11932ca780678fd98f327d3304c4e4468b`
